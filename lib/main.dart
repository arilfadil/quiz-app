import 'package:flutter/material.dart';
import 'package:quiz_app/quiz.dart'; // Pastikan path ini benar dan mengarah ke file quiz.dart Anda

void main() {
  runApp(const MaterialApp(
    home: Quiz(), // Menjalankan widget Quiz sebagai halaman utama aplikasi
  )); 
}
